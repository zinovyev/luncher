%h1 Registered Users

- Organization.list_public do |organization|
  %div{class: 'panel panel-default'}
    %div{class: 'panel-heading'}
      Organization:
      %b #{organization.title}
    %table{class: 'table table-striped'}
      %thead
        %tr
          %th Name
          %th Username
          %th Email
          %th Organization
          %th Approved
          %th
          %th
      %tbody
        - @users.list_public(organization.id) do |user|
          %tr
            %td= user.name
            %td= user.username
            %td= user.email
            %td= user.organization.title
            %td= user.approved?
            %td= link_to 'Edit', edit_admin_user_path(user), class: 'btn btn-primary'
            %td= link_to 'Destroy',
              admin_user_path(user),
              method: :delete,
              data: { confirm: 'Are you sure?' },
              class: 'btn btn-danger'
