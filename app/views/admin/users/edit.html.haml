%h2 Edit #{@user.name.to_s.humanize}
= form_for(@user,
  as: @user_name,
  url: edit_admin_user_path(@user),
  html: { method: :put }) do |f|
  = render 'devise/registrations/form', f: f, resource: @user, submit_text: 'Edit'

= link_to 'Back', admin_users_path
