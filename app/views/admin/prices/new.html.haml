%h1 Create new price

= simple_form_for @price,
  url: admin_organization_prices_path(organization: @organization) do |f|
  = render 'form', f: f

= render 'shared/back_button', url: admin_organizations_path

