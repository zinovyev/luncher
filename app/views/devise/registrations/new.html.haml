%h2 Sign up
= form_for(resource,
  as: resource_name,
  url: registration_path(resource_name)) do |f|
  = render 'form', f: f, submit_text: 'Sign Up', organization_select: true
= render 'devise/shared/links'
